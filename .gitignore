# Environment
.env

# Node / API
server/node_modules/
server/dist/
server/uploads/

# Flutter
app/.dart_tool/
app/build/
app/.flutter-plugins*
app/.packages
app/pubspec.lock
app/android/
app/ios/
app/linux/
app/macos/
app/windows/
app/test/

# Flutter web is scaffolded by `flutter create` at build time; only our brand
# assets (printer favicon + PWA/launcher icons + manifest) are tracked.
app/web/*
!app/web/favicon.png
!app/web/manifest.json
!app/web/icons/
!app/web/icons/*

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
